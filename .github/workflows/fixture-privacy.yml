name: Fixture privacy

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  fixture-privacy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Validate sanitized fixtures
        run: bash scripts/check-fixture-privacy.sh
